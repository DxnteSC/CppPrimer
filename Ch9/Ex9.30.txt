Question: What, if any, restrictions does using the version of resize that takes a single argument place on the element type?

Answer: If any new elements are added, then those elements must have a default constructor.