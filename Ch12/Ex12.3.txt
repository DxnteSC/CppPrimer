Question: Does this class need const versions of push_back and pop_back? If so, add them. If not, why aren’t they needed?

Answer: No, because push_back and pop_back add and remove elements, therefore should not be const.