Question: Would the pointerlike version of HasPtr benefit from defining a swap function? If so, what is the benefit? If not, why not?

Answer: No, because the pointers are already swapped by the std version of swap. 