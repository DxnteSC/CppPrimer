Question: In this section, we noted that it was not only illegal but meaningless to try to add two pointers. Why would adding two pointers be meaningless?

Answer: As pointers point to positions in memory, adding two pointers would jump to a random position in memory.